Recommended for VSCode
Must have Java pre-downloaded otherwise game will not launch.

Copy and Paste the code into VSCode(recommended) or some other compiler, and save the code. Run the code in whatever terminal you have(must have Java predownloaded). Game will launch automatically.
